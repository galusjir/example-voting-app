pipeline {
    agent any

    tools {
      nodejs 'NodeJS 8.9.0'
    }

    stages {
        stage('Build') {
            steps {
                echo 'Compiling voting app'
                dir('result') {
                    sh 'npm install' 
                }
            }
        }

        stage('Test') {
            steps {
                echo 'Testing voting app'
                dir('result') {
                    sh 'npm install'
                    sh 'npm test'
                }
            }
        }

        
    }
}
